# OCR Grid Mapper

A Python-based utility designed to retrieve fragmented spatial data from a published Google Doc and reconstruct it into a visual message. The tool parses a coordinate-based table, maps Unicode characters to a 2D grid, and handles axis inversion to render a correctly oriented secret message in a fixed-width font. This project demonstrates proficiency in web scraping with BeautifulSoup, data parsing, and coordinate system manipulation.

